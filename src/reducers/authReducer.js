/*  */

const initialState = {
  checking: true,
  //   uid
  //   name
};

export const authReducer = (state = initialState, action) => {
  switch (action.types) {
    default:
      return state;
  }
};
